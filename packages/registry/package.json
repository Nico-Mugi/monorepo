{
  "name": "@repo/registry",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./registry.json": "./registry.json"
  },
  "scripts": {
    "sync": "node scripts/sync-from-ui.mjs",
    "build": "node scripts/sync-from-ui.mjs && shadcn build -o ./public/r",
    "clean": "rimraf public registry"
  },
  "dependencies": {
    "@repo/ui": "workspace:*"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "shadcn": "^4.16.1",
    "typescript": "^6.0.3"
  }
}
