node_modules
dist
.output
.wrangler
.turbo
*.log
*.tsbuildinfo
.env
.env.local
.env.*.local

# Generated shadcn registry output — rebuilt by `pnpm build --filter @repo/registry`
packages/registry/public/
# Generated registry component sources, synced from @repo/ui — see packages/registry/scripts/sync-from-ui.mjs
packages/registry/registry/

# Generated by `wrangler types` (pnpm cf-typegen) — regenerate after editing wrangler.jsonc bindings
worker-configuration.d.ts

# Generated by scripts/compile-cv-pdf-css.mjs, run as part of dev/build — see apps/portfolio/package.json
apps/portfolio/src/server/cv-pdf.generated.css

# OS
.DS_Store
Thumbs.db

# Private package — skills, memory, and sensitive config. Own private GitHub repo.
# Clone instructions are in the install skill (packages/private/.claude/skills/install.md).
packages/private/

# Private apps — each lives in apps/<name> but has its own GitHub repo.
# Clone them manually: git clone git@github.com:Nico-Mugi/<name>.git apps/<name>
# Add entries here when you create a new private app:
# apps/my-private-app/
